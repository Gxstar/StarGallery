# 2026-08-11 工作日志

## minSdk 提升改造方案（docs/minsdk35-migration-plan.md）

用户提出将 minSdk 30 → Android 15 以获取 AVIF/HEIF/JXL 原生支持。经源码走查（15 处 SDK_INT 分支、7 个文件）与官方文档核实：

- **关键事实修正**：JXL 永无 Android 原生支持（官方媒体格式表无此格式），必须保留 jxl-coder；AVIF 解码 API 34 起强制原生，10-bit/HDR 显示（hasGainmap、COLOR_MODE_HDR）也是 API 34；API 35 仅多 ISO 21496-1 gain map（Ultra HDR 1.1）与 HDR headroom。
- **决策建议**：推荐 minSdk 34（覆盖 54.5%，分支可清零，未来升 35 只改一个数字）；minSdk 35 覆盖 41% 可选。
- **紧急项**：Play 要求 2026-08-31 起 targetSdk ≥ 36，项目现为 35，需先升级。
- **改造要点**：移除 avif-coder（AVIF/HEIF 走原生）、AvifRegionDecoder 重构为 ImageDecoder、权限三档合并（建议去掉 READ_MEDIA_VISUAL_USER_SELECTED 避免部分授权降级）、移除 READ_EXTERNAL_STORAGE、desugar/splashscreen 可评估移除。
- 交付：完整分阶段方案文档（6 阶段 + commit 拆分 + 回归矩阵 + 风险表）。

后续若执行改造，按方案中的 commit 拆分逐个落地，每个 commit 可独立回退。

## minSdk 决策定稿（v2，同日下午）

- 用户拍板：**minSdk = 35（Android 15）**，JXL 单独走 jxl-coder，其余格式全走系统原生解码，HDR 效果必须正确保留。
- 走查发现 3 个 P0 级 HDR 缺陷（已写入方案第七、八章）：
  1. `Photo.isUltraHdr` 只是 `mimeType=="image/jpeg"`，`shouldProbeHdr` 只认它 → AVIF 10-bit / HEIF gain map 永远进不了 HDR 路径；
  2. Glide Downsampler 会把高位深降为 ARGB_8888、采样缩放可能丢 gainmap → 高位深内容必须走 ImageDecoder 直接路径；
  3. `MediaScanner` 扫描期已探测的 `Photo.isHdr` 字段详情页未使用，还在每张 JPEG 重复 200x200 探测（浪费且不一致）。
- 修复方向：HDR 候选条件扩为 `isUltraHdr || isAvif || isHeic`，探测结果按 `hasGainmap || isWideGamut` 判断；优先复用 `photo.isHdr`。
- P1 坑：JXL 16-bit RGBA_F16 大图内存峰值（4096 长边 ≈ 89-134MB，上限建议降至 2048-2560）、ViewPager2 相邻页 colorMode 竞争闪烁、AvifRegionDecoder 严格按 sampleSize 缓存导致缩放反复全图解码。
- 方案文档已更新至 v2（`docs/minsdk35-migration-plan.md`），含解码决策树与 13 条坑点分级清单。

## 方案自评审（v3，同日晚些时候）

对解码展示方案做了批判性复核，文档新增"九、方案自评审"章节，4 项修订：

- **R1**：AvifRegionDecoder 改 ImageDecoder 必须 `setTargetColorSpace(SRGB)`——10-bit AVIF 会被 ImageDecoder 输出为 RGBA_F16（8B/px），tile 解码内存翻倍反而比 HeifCoder 更差；转换到 sRGB 输出 8-bit tile 才不倒退。
- **R2**：`photo.isHdr` 对 AVIF/HEIF 恒为 false（`detectUltraHdr` 仅检 JPEG），"复用扫描期标记"只对 JPEG 生效 → 加详情页探测结果缓存（LruCache）避免滑动重复探测，不扩展扫描器。
- **R3**：HEIF gain map（ISO 21496-1）在 ImageDecoder 上能否 hasGainmap==true 官方未明确背书，需样本实测；不可行则降级 isWideGamut→WIDE。
- **R4**：loadHdrBitmap 需泛化为"原生解码（HDR/WIDE）"，探测三档分流。

评审结论：方案合理、精简（最小必要改动 ~100 行级），可靠性有条件通过，8 项实测验证清单待落地前执行。

## 大图解码链路评估（v4，同日）

用户追问 Glide/子采样对大体积 AVIF/HEIF 的限制与更先进方案。核实到关键事实：

- **`BitmapRegionDecoder` 的 AVIF 区域解码是 2025-09-17 才合入 Skia**（commit "BitmapRegionDecoder: Enable AVIF support"，Bug 435430895），对应 Android 16（API 36）+。**minSdk 35 下 AVIF 无原生 tile 解码，自定义 AvifRegionDecoder 必须保留**（平台边界）。
- HEIF 的 BitmapRegionDecoder 支持更早（Skia kHEIF）→ HEIF 大图可走 ZoomImage 内置原生子采样，不需要 avif-coder；内置失败再注册 AvifRegionDecoder 兜底。
- 限制诊断：Glide 8-bit 降级/2 幂采样/无 F16 池（只适合列表预览）；自定义解码器整帧解码+裁切、缩放频繁全图重解。
- 先进方案分层：A（本次内）：HEIF 原生子采样 + AvifRegionDecoder v2（SRGB tile + 多级采样缓存 512/1024/2048 + 容差）+ 渐进加载（缩略图→2K→全图）+ 原生硬件加速（HEVC/AV1）；B（后续）：多分辨率金字塔磁盘缓存 + FileChannel.map；C：HEICS/AVIF 序列用 MediaCodec；D：HDR 展示平台最优链路 = ImageDecoder + colorMode + headroom。
- 方案文档更新至 v4（新增第十章）。

## 技术选型定稿（v5，同日）

用户最终需求：JXL 单独处理（由我定方案）；AVIF/HEIF 原生 + HDR 展示 + 子采样；现有栈不行就找替代库。

核实与定稿（文档新增第十一章）：

- **ZoomImage README 无内置 AVIF 区域解码**；Glide/Coil/Fresco/libavif 均无 AVIF region/tile 解码 API（AV1 帧内编码格式本质），Google Photos 也是整帧解码 + 多分辨率缓存 → **不需要换库**。
- 需求满足矩阵：HEIF 三项全原生（BitmapRegionDecoder 子采样 + ImageDecoder HDR）；AVIF 原生解码/HDR ✅，子采样 ⚠️ 自定义（API 36+ 才原生，平台边界）。
- **JXL 定稿**：识别用 Photo.isJxl（MIME+扩展名）；缩略图 ThumbnailManager 走 jxl-coder 核心库生成 512px 落盘；Glide 兜底走 jxl-coder-glide；详情页 loadJxlDirect（DEFAULT 自动 8/16-bit）；16-bit 解码上限 2560（防 F16 OOM）；无 EXIF、无子采样为已知限制。
- 最终技术栈：Glide 5 + ZoomImage 1.6 + ImageDecoder/BitmapFactory 原生（AVIF/HEIF）+ jxl-coder（JXL）+ AvifRegionDecoder v2（重写）；移除 avif-coder。

## 高位深 10/12-bit 评估（v6，同日）

用户要求支持 10/12-bit AVIF/HEIF。评估结论（文档新增第十二章）：

- **10-bit ✅ 全原生**：解码（AVIF API 34+ 强制 / HEIF Main10 API 28+）+ RGBA_F16 + HDR/WIDE colorMode 显示；HEIF gain map（21496-1）API 35 待实测（R3）。
- **新增修订 R5**：colorModeForBitmap 需恢复 PQ/HLG transfer 检测——BT2020_PQ/BT2020_HLG 应走 COLOR_MODE_HDR 而非 WIDE（仅 isWideGamut 会误判为广色域 SDR）。项目 AGENTS.md 三重检测正是为此，v3 简化时勿丢。
- **12-bit ❌ 不支持**，三层理由：①官方 AVIF 只保证 baseline（12-bit 解码不保证，Oppo 定制 ROM 佐证）；②Android 位图高位深只有 RGBA_F16，无 12-bit 格式；③无 12-bit 显示面板（消费级最高 10-bit，8-bit+FRC 为主）。12-bit 文件正确做法=降级显示，不引入兜底库。

## minSdk 35 改造动工（同日下午，已完成）

用户确认开始动工，按方案 v3-v6 落地全部代码改动，**编译 + 单测通过**：

- build.gradle.kts：minSdk 30→35，targetSdk 35→36（Play 8-31 强制）；移除 avif-coder/avif-coder-glide（toml 同步）
- 删除 15 处 SDK_INT 分支（MainActivity cutout、MediaScanner 2 守卫、PhotoPageViewHolder 5 处 HDR、三 Fragment 权限三档、AvifRegionDecoder checkSupport）
- AvifRegionDecoder 重写：HeifCoder → ImageDecoder 原生 + setTargetColorSpace(SRGB)（R1，tile 8-bit 防 F16 内存翻倍）
- HDR 链路：shouldProbeHdr 扩为 isUltraHdr||isAvif||isHeic；探测 hasGainmap||isWideGamut；photo.isHdr 跳过重复探测；colorModeForBitmap 恢复 BT2020_PQ/BT2020_HLG→COLOR_MODE_HDR（R5）
- 权限：三处单档化（仅 READ_MEDIA_IMAGES/VIDEO，Photos 加 ACCESS_MEDIA_LOCATION），移除 VISUAL_USER_SELECTED 与 READ_EXTERNAL_STORAGE（Manifest + 权限说明页 + 中英 strings 同步清理）
- lint：**NewApi 错误 0**；存量 15 错误未动（3 处 FragmentBackPressedCallback 行为相关 + 12 处 MissingTranslation 内容翻译）；顺手修 2 处 UseAppTint
- 验证：assembleDebug BUILD SUCCESSFUL（APK 55MB）、testDebugUnitTest 1/1 通过
- 遗留待办：R3 实测 HEIF gain map（API 35 原生解码确定性）、10-bit 样本真机验证、渐进加载/多级缓存（v4 阶段 A 的架构项，未在本轮实现）

## 存量 lint 错误清理（同日下午，已完成）

用户要求清理存量 lint 错误，已全部修复并 **lintDebug 全绿（0 errors，316 warnings）**：

- 3 处 FragmentBackPressedCallback：AlbumDetailFragment / HiddenFragment / TrashFragment 的 `addCallback(this, ...)` → `addCallback(viewLifecycleOwner, ...)`（均在 onViewCreated 注册，viewLifecycleOwner 可用，安全）
- 12 处 MissingTranslation：values-en 补齐"图片编辑"整段英文翻译（save/saved/save_failed/save_crop_confirm/edit_raw_unsupported/edit_video_unsupported/crop/adjust/flip/filter/rotate/redo）
- 此前已修 2 处 UseAppTint（layout_scanning_progress.xml）

至此 minSdk 35 改造 + lint 全绿：17 文件 +127/-176。下一步：真机验证矩阵（R3 HEIF gain map、10-bit 样本、ViewPager 快速翻页 colorMode），然后可拆 commit 提交。

## commit 拆分提交（同日下午，已完成）

真机验证通过后按方案分组提交，5 个 commit 全部落地，工作区干净：

- `62d3694` build: minSdk 提升至 35、targetSdk 提升至 36，移除 avif-coder 改用系统原生解码（build.gradle/toml/AvifRegionDecoder/ThumbnailManager）
- `b49ef03` refactor: 删除全部 SDK_INT 分支，HDR 探测扩展至 AVIF/HEIF（MainActivity/MediaScanner/PhotoPageViewHolder）
- `dcd20aa` chore: 统一媒体权限，移除 READ_MEDIA_VISUAL_USER_SELECTED 与 READ_EXTERNAL_STORAGE（三 Fragment/Manifest/权限页/strings）
- `7cf0958` chore: 修复存量 lint 问题（back 回调、app:tint、缺失英文翻译）
- `4ac430b` docs: 新增 minSdk 35 迁移方案与工作日志

后续演进方向（未做）：渐进加载 + 多级采样缓存（v4 阶段 A）、HEIF 动图/AVIF 序列（MediaCodec）、升 minSdk 36 后删 AvifRegionDecoder。

## AVIF 回归 avif-coder（v7，同日晚些时候）

用户反馈 10-bit HDR AVIF 灰图 + 无缩略图。解析样本 `C:\Users\gengx\Desktop\01045811.avif` 的 av1C box：

- **profile 1 (High) / 10-bit / 4:4:4 色度采样** → Android 原生解码器只保证 baseline（4:2:0 8/10-bit），4:2:2/4:4:4 与 12-bit 直接解码失败 → 灰图 + 无缩略图。改造前 libavif 能解，原生不能——"原生化"支持面收窄的实证。

用户拍板 AVIF 单独走 avif-coder，已完成（编译通过）：

- 恢复 avif-coder + avif-coder-glide 依赖（toml + build.gradle）
- AvifRegionDecoder 换回 HeifCoder（getSize + decodeSampled，libavif 支持 4:4:4/12-bit）
- 详情页 isHdrCandidate 移除 isAvif（AVIF 走 Glide + AvifRegionDecoder，不再走 ImageDecoder HDR 探测）
- 缩略图经 avif-coder-glide 恢复生成
- 方案文档更新 v7（新增第十三章）；HEIF/JPEG/JXL 保持现状
- HDR 保留度待真机确认（avif-coder 支持 RGBA_F16 输出，colorModeForBitmap 仍按位图属性处理）

## AVIF 仍灰 → 直连 HeifCoder（v7.2，同日）

恢复 avif-coder 后用户反馈**依然灰图**。深挖发现：

- 反编译 avif-coder-glide 2.2.1：只注册 Stream/ByteBuffer 解码器（handles 用 HeifCoder.isSupportedImage），**Glide 加载 content:// Uri 存在 FD/Stream 路径不确定性**，4:4:4 样本可能根本走不到 libavif 解码器 → 依旧回退 BitmapFactory 失败
- PreferredColorConfig.DEFAULT(1) = 库自动选最佳色彩空间（10-bit 源 → RGBA_F16 保留 HDR）

**修复（对齐 JXL 模式，已编译通过）**：AVIF 详情页 + 缩略图**直连 HeifCoder 核心库字节流解码**，完全绕开 Glide content:// 链路：

- PhotoPageViewHolder：loadImage 加 `photo.isAvif → loadAvifDirect`（读流 → HeifCoder().decodeSampled(bytes, target, DEFAULT) → setImageBitmap + colorModeForBitmap）；AVIF 禁子采样（同 JXL）；解码上限 **2560**（DEFAULT 下 10-bit 输出 F16=8B/px，防 OOM）
- ThumbnailManager：isAvif → decodeAvifThumbnail（HeifCoder decodeSampled 512px，RGBA_8888）
- avif-coder-glide 保留作 Glide 路径兜底（不依赖）
- 注意：JXL 详情上限仍是 4096（v5 定的 2560 未落地代码），F16 内存风险待后续统一

待用户真机验证：4:4:4 样本缩略图 + 详情显示；日志 cfg=RGBA_F16/cs=BT2020 确认 HDR 保留。

## 修复分派顺序（v7.3，同日）

用户贴出 logcat 栈帧含 `loadHdrBitmap$1.invokeSuspend`——证明 AVIF 走错路径：`shouldProbeHdr`（isHdr 误标/MIME 误判为 JPEG 时命中）在 `photo.isAvif` **之前**，AVIF 被送进 ImageDecoder HDR 探测 → 原生解 4:4:4 失败 → 灰图，且 `loadAvifDirect` 日志从未出现。

**修复**：`photo.isAvif` 分支提到分派最前，AVIF 强制直连 libavif，永不进入 HDR 探测。已编译通过，待用户重新验证（logcat 应出现 `loadAvifDirect OK cfg=... cs=...`）。

## AVIF 修复验证成功（v7.3 确认，同日）

用户 logcat 确认：`loadAvifDirect OK ... cfg=RGBA_F16 cs=scRGB-nl (id=2) w=1920 h=2560`
- ✅ 走对路径（直连 libavif）、解码成功（不再灰）
- ✅ **RGBA_F16 = 10-bit 高位深完整保留**
- cs=scRGB-nl（id=2，扩展 sRGB 工作空间，libavif 将 PQ/HLG 内容转换至此）→ `isWideGamut=true` → 窗口切 **WIDE 广色域模式**显示（非 HDR 模式：无 gainmap 且非 BT2020_PQ/HLG，R5 分支不命中——这是正确行为）
- 后续优化方向：JXL 详情上限 4096→2560 统一（同 F16 风险）

## HDR AVIF 双路径自适应（v8，同日）

用户指出：Lightroom 导出 HDR REC.2020（PQ+BT.2020）为何不 HDR 显示。查实：

- **根因**：libavif 解码 10-bit HDR 源输出 **scRGB-nl + RGBA_F16**（把 PQ/BT.2020 转换到扩展 sRGB 工作空间）；avif-coder 的 Kotlin API 未暴露色彩空间/toneMapper 选项（decodeImpl 仅 clrConfig/scaleMode/scaleQuality，ToneMapper 枚举未接入公共 API）→ 无法保留 PQ 色彩空间 → App 检测不到 BT2020_PQ/HLG 信号，且 avif-coder 不附加 gainmap → 只能 WIDE 广色域显示（颜色准确、位深保留、无 HDR 高光）。
- **矛盾**：原生 ImageDecoder 保留 HDR（gainmap/PQ）但解不了 4:4:4；libavif 解 4:4:4 但转 scRGB 无 HDR。

用户选**双路径自适应**（已实现并编译通过）：

- `loadImage`：`photo.isAvif → loadAvifAdaptive`
- `loadAvifAdaptive`：先原生 ImageDecoder 200px 探测 →
  * 成功且 HDR/宽色域 → `loadHdrBitmap`（原生，真 HDR/WIDE）
  * 成功且 SDR → Glide + AvifRegionDecoder 子采样
  * 原生抛异常（4:4:4/12-bit）→ `loadAvifDirect`（libavif，WIDE 保底）
- `configureSubsampling` 恢复只禁 JXL（AVIF 原生 SDR 路径仍用 AvifRegionDecoder 子采样）
- `loadAvifDirect` 显式禁子采样 + 注释说明 scRGB 局限
- 待验证：4:4:4 走 libavif fallback；Lightroom 若导出 4:2:0 HDR AVIF 走原生真 HDR

## JXL/AVIF 网格缩略图修复（v8.1，同日）

用户反馈 JXL/AVIF 网格缩略图都不显示。排查网格链路：

- `PhotoGridViewHolder.loadImage`：`thumbnailPath` 缓存文件存在才 load(File)（JPEG 原生 OK），否则回退 `content://` Uri（JXL/AVIF 在 Glide 的 FD/Stream 路径失败）→ **缩略图缓存是网格正常显示的关键**
- **JXL 确定性 bug**：`ThumbnailManager.generateThumbnail` 的 RAW 过滤 `mimeType.startsWith("image/x-") && mimeType != "image/jxl"` 会把 **`image/x-jxl`**（部分系统给 .jxl 的 MIME）误拦 → 缩略图不生成。已修复：先算 `isJxl`/`isAvif`（MIME+扩展名兜底），`!isJxl && !isAvif && image/x-*` 才跳过
- AVIF 缩略图未定位到确定 bug（HeifCoder 直连应能生成），待日志确认（ThumbnailManager tag）
- 已编译通过；用户验证：装新 APK → 触发全量扫描补生成缩略图 → 看 PhotoGrid/ThumbnailManager 日志

## 4:4:4 AVIF 缩略图修复（v8.2，同日）

用户确认：AVIF 4:2:0 缩略图正常，**4:4:4 那张无缩略图**。定位：

- `decodeAvifThumbnail` 用 `RGBA_8888` 配置，而 4:4:4 样本是 **10-bit** → avif-coder 对 10-bit HDR 源 + RGBA_8888 抛 **CorruptedBitDepthException**（源码异常类 + PreferredColorConfig 注释 "RGBA_8888 will clip out HDR"）→ 缩略图生成失败
- 详情页用 DEFAULT（输出 F16）所以能解——配置差异导致"详情能开、缩略图没有"

修复（已编译通过）：
- `decodeAvifThumbnail` 改 **DEFAULT** 自动配置，10-bit 源输出 F16 后统一 `copy(ARGB_8888)` 再压 JPEG（F16 不支持 JPEG compress）
- `decodeJxlThumbnail` 同样处理（16-bit JXL 有同类风险）
- 待用户验证：4:4:4 AVIF 缩略图恢复；16-bit JXL 缩略图（若之前也不显示）恢复
